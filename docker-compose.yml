name: mini-games

services:
  mini-games:
    build:
      context: .
      dockerfile: Dockerfile
    image: mini-games:${APP_VERSION}
    pull_policy: missing
    container_name: mini-games
    restart: always
    env_file:
      - src/assets/.env
    ports:
      - "8005:8080"
