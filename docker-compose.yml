name: minigames

services:
  minigames:
    build:
      context: .
      dockerfile: Dockerfile
    image: minigames:${APP_VERSION}
    pull_policy: missing
    container_name: minigames
    restart: always
    env_file:
      - src/assets/.env
    ports:
      - "8005:8080"
